# tile keybinds for skhd
# use virtual keycodes in Apple's HIToolbox/Events.h for special keys

# horizontal tile presets
rcmd + ralt - z : tile 0-1
rcmd + ralt - x : tile 1-2
rcmd + ralt - c : tile 2-3
rcmd + ralt - v : tile 3-4

rcmd + ralt - a : tile 0-2
rcmd + ralt - s : tile 1-3
rcmd + ralt - d : tile 2-4

rcmd + ralt - q : tile 0-3
rcmd + ralt - w : tile 1-4

rcmd + ralt - r : tile -

# vertical tile presets
rcmd + ralt - f : tile f
rcmd + ralt - t : tile t
rcmd + ralt - b : tile b
rcmd + ralt - y : tile y
rcmd + ralt - h : tile h
rcmd + ralt - n : tile n
rcmd + ralt - j : tile j
rcmd + ralt - k : tile k

# snap presets
rcmd + ralt - 0x32 : tile snap 0 # ` key
rcmd + ralt - 1    : tile snap 1
rcmd + ralt - 2    : tile snap 2
rcmd + ralt - 3    : tile snap 3
rcmd + ralt - 4    : tile snap 4
rcmd + ralt - 0    : tile snap -1

# move between multiple displays
rcmd + ralt - 0x21 : tile [
rcmd + ralt - 0x1E : tile ]

# undo
rcmd + ralt - u : tile undo
